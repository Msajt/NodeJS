//! Sockets
    //? => Opening in the computer system, which you can connect
    //?    to send and receive data
    //? => Polling:
        //* Period of time to make a request (setInterval)

//! Sockets vs. Polling
    //? emit (Sockets)
    //? setInterval (Polling)

//! Websockets
    //? Protocol
    //? Event Listeners
    //? socket.io - socket.io-client (server/client)

//! Namespace
    //? => Communication channel that allows you to split the
    //?    logic of your application over a single shared
    //?    connection
    //? => One socket for multiple purposes